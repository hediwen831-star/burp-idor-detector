# 安全政策

## 这个插件的特殊性

Burp 扩展运行在**使用者的 Burp 进程里**，能读到所有经过 Burp 的流量 ——
包括 Cookie、Token、请求体。这个位置本身就是高信任度的。

所以「插件自身的安全」有两层：

1. **不泄漏使用者的数据**（凭据、流量）
2. **不对目标造成意外影响**（这是「主动扫描插件」特有的风险）

---

## 数据安全设计

| 机制 | 说明 |
|---|---|
| **凭据脱敏** | 第二身份凭据在日志与 Issue 详情里只显示 `JSESSI****cdef` 形式 |
| **不落盘** | 插件不写任何文件，配置只存在内存（Burp 重启即失效） |
| **不外联** | 插件不向任何第三方服务器发请求 —— 唯一的外部请求是发往使用者正在测试的目标 |
| **不限权限** | 只用 Burp 提供的 Montoya API，不申请额外系统权限 |

**为什么脱敏这件事要认真做**：

第二身份的凭据是使用者另一个账号的**有效登录态**。而 Burp 的日志和
Issue 详情经常被截图贴进报告、issue、聊天窗口 ——
一个没脱敏的 Cookie 就等于把账号交出去了。

---

## 对目标的影响控制

主动扫描类插件最大的风险不是漏检，是**把目标搞出问题**。所以：

| 机制 | 默认值 | 目的 |
|---|---|---|
| **默认不发请求** | 关闭 | 装上后只做被动观察，用户必须显式启用 |
| 方法白名单 | GET / HEAD / OPTIONS | 避免误改业务数据（POST 可能是「提交订单」） |
| 限速 | 100ms 间隔 | 不让插件变成压测工具 |
| 单 host 配额 | 50 个候选 | 防止在中大型站点上刷出成百上千请求 |
| 范围控制 | 可指定只检测/排除的 host | 让使用者精确控制流量去向 |
| 请求去重 | method + host + path + 参数值 | 同一请求不重复检测 |

**第一条是最重要的。**

一个在后台自动发请求的安全插件，最大的风险是
「使用者以为它没在工作，实际它在发请求」。
所以默认值必须选安全的那一侧 —— 要发请求，得由人明确点头。

---

## 报告漏洞

如果发现**插件本身**的问题，请报告：

| 类型 | 例子 |
|---|---|
| 凭据泄漏 | 日志 / Issue 里出现了完整的凭据值 |
| 未授权的请求 | 未启用主动重放时仍然发了请求 |
| 流量放大 | 某个输入导致请求数远超配额 |
| 绕过方法限制 | 对 POST / PUT / DELETE 发了请求 |
| 解析崩溃 | 构造的响应能让插件抛异常、影响 Burp 稳定性 |

请通过 GitHub 的 [Private vulnerability reporting](../../security/advisories/new)
提交，或邮件 hediwen831@gmail.com。

**请不要开公开 issue** —— 一个「凭据泄漏」类问题被公开，
正在用它的人会立即处于风险中。

---

## 使用者须知

- 本插件仅用于**授权范围内**的安全测试
- 使用前请确认你的授权范围**包含「发起额外的自动化请求」**
- 插件内置了限速与配额，但它不能替你判断授权边界

---

## 支持范围

| 版本 | 支持状态 |
|---|---|
| v0.1.0 及 `main` 最新提交 | 接受报告 |
| 更早版本 | 请先确认能在最新版本上复现 |
